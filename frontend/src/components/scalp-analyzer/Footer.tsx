/**
 * Footer — disclaimer banner at the bottom of ScalpAnalyzer.
 *
 * Phase 4.D 분할: pages/ScalpAnalyzer.tsx → components/scalp-analyzer/Footer.tsx
 *
 * Static — no props needed.
 */

export function Footer() {
  return (
    <div className="scalp-footer">
      ⚠ 매매 보조 도구이며 투자 권유가 아닙니다. 모든 매매 결정의 책임은 본인에게 있습니다.
    </div>
  );
}
