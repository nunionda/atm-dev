# ATS Dependencies — Generated companion to pyproject.toml
#
# Source of truth: pyproject.toml [project.dependencies]
# 이 파일은 `pip install -r requirements.txt` 호환성을 위해 유지.
# 추가/수정 시 pyproject.toml도 함께 갱신할 것.
#
# Install:
#   pip install -e .                  # editable install (pyproject 사용)
#   pip install -r requirements.txt   # legacy

# Web framework (API server)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# HTTP client
requests>=2.31.0
httpx>=0.25.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Market data
yfinance>=0.2.40
ta>=0.11.0

# Time / timezone
pytz>=2024.1

# Database
SQLAlchemy>=2.0.0

# Configuration
PyYAML>=6.0
python-dotenv>=1.0.0

# Telegram notifications
python-telegram-bot>=20.0

# Scheduling
APScheduler>=3.10.0

# Logging
loguru>=0.7.0

# Testing (dev extras — also in pyproject [project.optional-dependencies.dev])
pytest>=7.4.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0
